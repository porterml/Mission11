@{
}

@model Book


<div class="card card-outline-primary m-1 p-1">
    <div class="bg-light p-1">
        <h4>@Model.Title</h4>        @*class="container-xl text-center"  These are pulled out in case i need them again later*@

        <span class="badge bg-info" style="float:right">
            Price: $@Model.Price
        </span>
    </div>

    <ul>
        @*class="container-fluid m-3 text border-bottom"   Saved for later reference*@
        <li>Author: @Model.Author</li>
        <li>Publisher: @Model.Publisher</li>
        <li>Isbn: @Model.Isbn</li>
        <li>Classification: @Model.Classification</li>
        <li>Category: @Model.Category</li>
        <li>PageCount: @Model.PageCount</li>
    </ul>


    <form method="post" asp-page="/Cart">

        @*use teh next two imputs to pass along values to identify books during a session so they can be added to the cart*@
        <input type="hidden" asp-for="@Model.BookId" />
        <input type="hidden" name="returnUrl" value="@ViewContext.HttpContext.Request.PathAndQuery()" />

        <span class="card-text">
            <button type="submit" class="btn btn-success btn-sm float-end">Add to Cart</button>
        </span>

    </form>
</div>


